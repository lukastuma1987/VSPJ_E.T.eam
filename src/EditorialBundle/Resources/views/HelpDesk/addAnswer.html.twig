{% extends '@Editorial/base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}Přidat odpověď na helpdesk zprávu{% endblock %}

{% block content %}
    <h1>{{ block('title') }}</h1>
    <p>{{ message.message }}</p>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Odeslat</button>
    {{ form_end(form) }}
{% endblock %}
